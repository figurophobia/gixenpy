# Copy this file to `.env` and fill in your Gixen.com credentials
# (your Gixen account's, NOT eBay's).
#
# `.env` is in .gitignore: it's never pushed to the repo. This file
# (`.env.example`) is, as a template, with no real credentials.
GIXEN_USERNAME=
GIXEN_PASSWORD=
